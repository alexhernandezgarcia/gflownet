defaults:
  - base

_target_: gflownet.envs.cube.Cube

id: cube
func: corners
# Dimensions of hypercube
n_dim: 2
# Maximum length of trajecotry
max_traj_length: 10
# Buffer
buffer:
  data_path: null
  train: null
  test:
    type: grid
    n: 1000
    output_csv: cube_test.csv
