defaults:
  - base

_target_: gflownet.envs.crystals.spacegroup.SpaceGroup

id: spacegroup

# Subset of space groups
space_groups_subset: null
# Stoichiometry
n_atoms: null
# Policy format
policy_fmt: onehot

# Buffer
buffer:
  data_path: null
  train:
    type: all
  test:
    type: all
