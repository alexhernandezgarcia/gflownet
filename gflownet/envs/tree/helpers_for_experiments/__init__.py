"""Helper scripts for launching, evaluating and aggregating tree experiments.

This __init__.py makes the folder a regular package so that the helpers can be
imported as ``gflownet.envs.tree.helpers_for_experiments.<module>`` after the
repo has been installed with ``pip install -e .`` (no sys.path hacking).
"""
